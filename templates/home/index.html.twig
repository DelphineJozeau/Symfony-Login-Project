{% extends 'base.html.twig' %}

{% block title %}Welcome{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            {% if app.user %}
                <div class="mt-5">
                    <h1 class="display-4 mb-4">Welcome, {{ app.user.email }}! 👋</h1>
                    <p class="lead text-muted mb-4">You are successfully logged in!</p>
                    
                    <div class="mt-4">
                        <a href="{{ path('app_logout') }}" class="btn btn-danger btn-lg">
                            Logout
                        </a>
                    </div>
                </div>
            {% else %}
                <div class="mt-5">
                    <h1 class="display-3 mb-4">Welcome to our Application! 🚀</h1>
                    <p class="lead text-muted mb-5">Please login or create an account to continue.</p>
                    
                    <div class="d-flex gap-3 justify-content-center flex-wrap">
                        <a href="{{ path('app_login') }}" class="btn btn-primary btn-lg">
                            Login
                        </a>
                        <a href="{{ path('app_register') }}" class="btn btn-success btn-lg">
                            Register
                        </a>
                    </div>
                </div>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}